import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
    colors: {
        color: {
            primary: "#263640",
            secondary: "#7C92A6",
            tertiary:  "#516373"
        }      
    }
})

export default theme