import styled, {css} from "styled-components";

const LogoSite = styled.h1`

    color: var(--primary);
    font-family: 'Alkatra', cursive;
    font-size: 1.56rem;

    @media (min-width: 500px){
        font-size: 1.9rem;
    }

    @media (min-width: 1000px){
        font-size: 2.18rem;
    }

` 

export default LogoSite